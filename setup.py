from setuptools import setup, find_packages

setup(
    name="Notion",
    version="0.1.0",
    description="Notion SDK for Python 3",
    author="sebastian",
    author_email="seba@cloudnative.co.jp",
    packages=find_packages(),
    install_requires=[
    ],
    entry_points={
        "console_scripts": [
        ]
    },
)
